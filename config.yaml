accounts:
  - flingjie
  - iFurySt

github:
  token: ${GITHUB_TOKEN}

llm:
  provider: openai
  model: deepseek-v4-pro
  api_key: ${OPENAI_API_KEY}
  base_url: ${LLM_BASE_URL}

weights:
  repo: 5.0
  commit: 3.0
  pr: 2.5
  issue: 1.5
  star: 1.0

output:
  dir: ./output
  formats:
    - markdown
    - json

compare:
  enabled: true
